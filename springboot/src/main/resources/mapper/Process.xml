<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.lengs.springboot.mapper.ProcessMapper">
    <select id="listByProcess" resultType="org.lengs.springboot.entity.Process">
        select * from Process
            <where>
                <if test="processID != null and processID != ''">
                    processID = #{processID}
                </if>
                <if test="processCreateBy != null and processCreateBy != ''">
                    and processCreateBy = #{processCreateBy}
                </if>
            </where>
    </select>
    <select id="listBySubmission" resultType="org.lengs.springboot.entity.Submission">
        select * from Submission where processID = #{ processID }
    </select>
    <insert id="createProcess" >
        insert into Process(processName,processCreateBy,groupID,processCreateTime,processChangeTime,processDeadTime,processCondition,processSubmission,processReport,processMission,processMidcheck,processOral)
        values (#{processName},#{processCreateBy},#{groupID},#{processCreateTime},#{processChangeTime},#{processDeadTime},#{processCondition},#{processSubmission},#{processReport},#{processMission},#{processMidcheck},#{processOral});
    </insert>
    <insert id="createSubmission" useGeneratedKeys="true" keyProperty="submissionID">
        insert into Submission(processID,submissionBase,submissionType,submissionBrief,submissionDirection,submissionLanguage,submissionTeacherReview,submissionExpertReview)
        values (#{processID},#{submissionBase},#{submissionType},#{submissionBrief},#{submissionDirection},#{submissionLanguage},#{submissionTeacherReview},#{submissionExpertReview})
    </insert>
</mapper>